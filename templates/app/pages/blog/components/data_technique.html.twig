<div>
    <p>Données techniques approximatives {{ isOnlyAller is defined ? "- aller" : "" }} : <br></p>
    <div class="font-medium flex gap-4 mt-2 text-red-500">
        {% if duration is defined %}
            <div>{{ duration }}</div>
        {% endif %}
        <div>{{ km }}km</div>
        <div>{{ d }}D+</div>
        {% if altitude is defined %}
            <div>{{ altitude }}m d'altitude</div>
        {% endif %}
    </div>
</div>
